clc;
clear;
close all;

syms x t

% symbolic
I1 = int(sin(x^2), x, 0, pi)
I2 = int(2*exp(2*t), t, -inf, 0)

% numerical
f = @(x) sin(x.^2);
I1_num = integral(f,0,pi)

g = @(t) 2*exp(2*t);
I2_num = integral(g,-Inf,0)
